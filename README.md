# mdex
A fully offline Markdown reader &amp; editor built on Tauri v2, for air-gapped / intranet / disconnected use. All files are processed locally — no network, no uploads. It supports live split preview, LaTeX math, code highlighting, images, tables, BibTeX citations, multi-tab editing, search &amp; replace and multi-format export.
