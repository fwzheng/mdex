<details>
<summary><b>🌐 Язык: Русский</b> — нажмите, чтобы выбрать другой язык</summary>

[English](README.md) · [简体中文](README.zh-CN.md) · [Français](README.fr.md) · [Deutsch](README.de.md) · [Español](README.es.md) · [Português](README.pt.md) · [Italiano](README.it.md) · **[Русский](README.ru.md)** · [日本語](README.ja.md) · [한국어](README.ko.md) · [العربية](README.ar.md)

</details>


# MDeX (macOS · Windows · Linux · Полностью автономно · Tauri v2)

> **MDeX** · произносится «эм-декс» (/ˌemˈdɛks/) — буква M, за которой следует «dex», два слога.

Автономный (offline-first) редактор и просмотрщик Markdown для использования **в изолированных сетях / интрасетях / без подключения к интернету**. Каждый файл обрабатывается локально — **без сети, без загрузки на сервер, без облачной синхронизации**.

- Единый автономный HTML-фронтенд (без Vue / React); Tauri v2 предоставляет только нативную оболочку (окна, меню, диалоги выбора файлов).
- **Нулевые сетевые запросы во время выполнения**: `marked` / `KaTeX` / `highlight.js` / `DOMPurify` / `mermaid` / `jsPDF` / `html2canvas-pro` / `turndown` / `@retorquere/bibtex-parser` и все шрифты KaTeX woff2 встроены / закодированы в base64 в единый `index.html`.
- Поддерживаются `.md` / `.markdown` / `.html`; приложение можно назначить обработчиком `.md` по умолчанию — открывайте двойным щелчком.

> Подходит для сценариев: **научные исследования / интрасети / защита конфиденциальности**. Без рекламы, без телеметрии, без передачи данных.

---

## 🌐 Языки

Интерфейс доступен на **11 языках**: English, 简体中文, Français, Deutsch, Español, Português, Italiano, Русский, 日本語, 한국어, العربية.

- Переключайтесь в любой момент через меню языка на панели инструментов; ваш выбор сохраняется между сессиями.
- **Арабский отображается письмом справа налево (RTL)** автоматически — основной текст, заголовки, маркеры списков и вся панель инструментов зеркально переносятся вправо; встроенные блоки кода, формулы LaTeX, английские термины и номера версий остаются слева направо и никогда не зеркалируются.
- Сам этот README переведён на все 11 языков — воспользуйтесь переключателем в самом верху страницы.

---

## ✨ Возможности

- **Многовкладочность + многооконность**: открывайте несколько файлов одновременно; несохранённые изменения помечаются точкой, при закрытии запрашивается подтверждение; закройте вкладку средним щелчком. Двойной щелчок по `.md` открывает собственное окно (по одному файлу на окно); двойной щелчок по уже открытому файлу **переводит фокус на это окно**, а не открывает его заново.
- **Живое разделённое предпросмотр**: перетаскивайте разделитель для изменения размера; кнопка на панели инструментов циклически переключает режимы «Разделённый / Редактор / Предпросмотр».
- **Переход по щелчку**: щёлкните в редакторе, чтобы прокрутить предпросмотр; щёлкните в предпросмотре, чтобы перевести курсор в редакторе.
- **Поиск и замена**: найти, заменить одно или все совпадения, с подсчётом количества.
- **Математика**: строчные `$…$` и блочные `$$…$$` (а также `\(...\)`, `\[...\]`), отображаются через KaTeX; длинные формулы переносятся по операторам или автоматически уменьшаются.
- **Подсветка кода**: язык определяется автоматически; подсветка в больших документах лениво выполняется по видимой области для плавности.
- **Диаграммы Mermaid**: блок ` ```mermaid ` отображается как блок-схема / последовательность / классы / состояния / диаграмма Ганта / круговая и т. д.
- **Изображения**: вставить / перетащить / выбрать — автоматически встраиваются как base64; работают и относительные локальные пути; по умолчанию изображения центрируются.
- **Таблицы**: таблицы GFM; узкие таблицы центрируются по содержимому, широкие прокручиваются по горизонтали без обрезки.
- **Цитирование (BibTeX)**: синтаксис `[@key]` / `\cite{key}`, числовой стиль; в конце формируется список ссылок с двусторонними переходами между в текстовой `[n]` и записью; поддерживается встроенный блок ` ```bibtex ` или отдельно загружаемый `.bib`.
- **Поддержка HTML**: открывайте файлы `.html` для отображения; конвертируйте между HTML и Markdown.
- **Тема / язык**: тёмная / светлая, **11 языков интерфейса** (中文, English, Français, Deutsch, Русский, Italiano, 日本語, 한국어, Español, Português, العربية — арабский автоматически справа налево).
- **Автосохранение черновика**: содержимое периодически сохраняется и восстанавливается после непредвиденного закрытия / сбоя.
- **Подсчёт слов**: в строке состояния отображаются символы / строки / слова в реальном времени, а также текущие строка и столбец.
- **Перетаскивание**: перетащите файл `.md` на окно, чтобы открыть его; перетащите изображение, чтобы вставить его.
- **Экспорт**: сохранение как Markdown / HTML / PDF (векторный + растровый) / LaTeX.

---

## ⌨️ Горячие клавиши

Используйте `⌘` на macOS, `Ctrl` на Windows / Linux.

| Горячая клавиша | Действие |
| --- | --- |
| `⌘/Ctrl + N` | Создать |
| `⌘/Ctrl + O` | Открыть файл |
| `⌘/Ctrl + S` | Сохранить |
| `⌘/Ctrl + Shift + S` | Сохранить как |
| `⌘/Ctrl + W` | Закрыть вкладку |
| `⌘/Ctrl + Shift + W` | Закрыть окно |
| `⌘/Ctrl + F` | Найти |
| `⌘/Ctrl + H` | Заменить |
| `⌘/Ctrl + B` / `I` / `E` | Полужирный / Курсив / Строчный код |
| `⌘/Ctrl + K` | Вставить ссылку |
| `Tab` | Отступ на 2 пробела |
| `Alt/Option + перетаскивание` | Прямоугольное (колоночное) выделение |
| `Alt/Option + Shift + ←↑↓→` | Расширить колоночное выделение |
| `Esc` | Отменить колоночное выделение |

> При нескольких открытых окнах горячие клавиши действуют только на окно в фокусе.

---

## 📝 Краткая шпаргалка

**Markdown**: заголовки `# / ## / ###`, полужирный `**текст**`, курсив `*текст*`, зачёркивание `~~текст~~`, строчный код `` `код` ``, блоки кода (тройные обратные кавычки с указанием языка по желанию), цитата `> текст`, списки `- / 1.`, список задач `- [ ] / - [x]`, ссылка `[текст](url)`, изображение `[alt](url)`, разделитель `---`, таблицы `| A | B |`.

**Математика**: строчно `$E = mc^2$`; блочно `$$\int_0^1 x\,dx$$` (может занимать несколько строк). Используется синтаксис LaTeX, отображается через KaTeX; `$` внутри блоков кода не считается разделителем формул. Поддерживаются окружения `align` / `aligned`, матрицы, `cases` и другие распространённые.

**Цитирование**: напишите в тексте `[@key]` или `[@a; @b]` (совместимый с LaTeX `\cite{key}`), вставьте библиотеку через блок ` ```bibtex ` или загрузите `.bib` кнопкой «Ссылки». В конце формируется список ссылок; `[n]` в тексте кликабельны.

---

## 📤 Экспорт (Сохранить как)

Нажмите «Сохранить как» и выберите формат:

- **Markdown (.md)**: сохранить исходник и обновить имя / путь текущей вкладки.
- **HTML (.html)**: автономный HTML со встроенным CSS + подсветкой кода; формулы сохранены как литерал `$…$` и автоматически отображаются встроенным KaTeX.
- **PDF векторный**: системный диалог печати, векторный вывод, чёткий при любом масштабе. Выберите «Сохранить как PDF».
- **PDF растровый**: файл PDF генерируется напрямую (без диалога), с низким / средним / высоким разрешением; разбивка по страницам на границах блоков (без разрыва формул / заголовков / кода).
- **LaTeX (.tex)**: преобразуется в компилируемый исходник `.tex` (с documentclass и пакетами; формулы сохраняются как есть). Экспортируется копия.

---

## 🔒 Автономность и безопасность

- **Нулевые сетевые запросы во время выполнения.** Артефакт сборки `dist/index.html` самопроверяется: отсутствуют внешние ссылки `src=` / `href=` / `url()` / `@import`.
- Строгий CSP (только локальный IPC, без WAN); все файлы читаются и пишутся локально, ничего не передаётся.
- Проверка: отключите Wi-Fi / отсоедините кабель и запустите — формулы, изображения, подсветка кода и Mermaid продолжают работать.
- В `dist/index.html` всё ещё присутствует около десятка строк `https://github.com/…`; все они находятся внутри **лицензионных / исходных комментариев** библиотек `marked` / `highlight.js` и т. п. — это простой текст, который **никогда не инициирует запрос**; оставлены без изменений из уважения к лицензиям open-source.

---

## 📦 Установка

### Готовые сборки
Скачайте установщик для вашей платформы из [Releases](./): macOS (`.dmg`, универсальный arm64 + x86_64), Windows (`.exe` / `.msi`), Linux (`.deb` / `.AppImage`).

### Открытие неподписанного приложения на macOS (обход Gatekeeper)
Это приложение не имеет подписи разработчика / не нотаризовано (в автономных сценариях нотаризация онлайн обычно невозможна). Первый запуск блокируется — выберите вариант:

- **CLI (рекомендуется)**: перетащите `.app` в Applications, затем
  ```bash
  xattr -dr com.apple.quarantine "/Applications/MDeX.app"
  ```
- **GUI**: в Finder щёлкните правой кнопкой по `.app` → «Открыть» → снова «Открыть» в диалоге; либо «Системные настройки → Конфиденциальность и безопасность» → прокрутите вниз → «Всё равно открыть».

---

## 🛠️ Сборка из исходников

### Однократная настройка (macOS)
```bash
xcode-select --install                       # Xcode command-line tools
rustup target add aarch64-apple-darwin x86_64-apple-darwin   # both needed for universal
npm install                                  # Tauri cli
npm run fetch                                # download frontend deps into vendor/ (online only here)
```

### Локальная разработка
```bash
npm run tauri dev        # builds dist/index.html, then launches the app window
```

### Сборка
```bash
# Apple Silicon only (faster)
npm run tauri build

# Universal binary (Apple Silicon + Intel, for distribution)
npm run tauri build -- --target universal-apple-darwin
```

Результат:
```
src-tauri/target/universal-apple-darwin/release/bundle/
├── macos/MDeX.app
└── dmg/MDeX_1.1.0_universal.dmg
```

### Windows / Linux / кросс-платформенно
- **Нативная сборка Windows** (создаёт установщик NSIS `.exe`): см. [BUILD-WINDOWS.md](./BUILD-WINDOWS.md).
- **Linux / macOS Intel / прочие кросс-платформенные варианты**: см. [BUILD-CROSS.md](./BUILD-CROSS.md).

Фронтенд `dist/index.html` не требует изменений между платформами; достаточно настроить упаковку под целевую ОС (`tauri.conf.json` `bundle.targets` добавляет `nsis` / `deb` / `appimage`, а также системные зависимости наподобие WebView2 / webkit2gtk).

---

## 📁 Структура проекта

```
markdown/
├── app-shell.html          # frontend source (HTML+CSS+JS, all app logic)
├── tools/
│   ├── fetch-vendor.mjs    # one-time: download deps into vendor/ (online only here)
│   └── build-html.mjs      # inline vendor into dist/index.html (KaTeX fonts → base64)
├── dist/index.html         # build output: fully offline single file (Tauri frontendDist)
├── vendor/                 # download cache (.gitignore)
├── package.json            # @tauri-apps/cli + scripts
└── src-tauri/
    ├── Cargo.toml          # tauri 2 + tauri-plugin-dialog / single-instance
    ├── build.rs            # tauri_build::build()
    ├── tauri.conf.json     # 1200×750 window, strict CSP, icons, .md association, menu hooks
    ├── capabilities/default.json
    ├── icons/              # full icon set from `cargo tauri icon`
    └── src/{main.rs, lib.rs}   # menus + file IO + multi-window routing
```

---

## 🎨 Кастомизация

| Что изменить | Где |
| --- | --- |
| Имя приложения / Bundle ID | `src-tauri/tauri.conf.json` → `productName` / `identifier` |
| Размер окна | `tauri.conf.json` → `app.windows[0]` (по умолчанию 1200×750) |
| Иконки | замените исходное изображение, затем `npm run icon` |
| Цвета темы / шрифты | CSS-переменные `:root` в начале `app-shell.html` |
| Пункты меню | `build_menu()` в `src-tauri/src/lib.rs` |
| Текст интерфейса / документ справки | `I18N` / `HELP_STRINGS` в `app-shell.html` |
| Версии зависимостей | `VERSIONS` в начале `tools/fetch-vendor.mjs` (затем `npm run fetch -- --force`) |

---

## 📄 Лицензия

Собственный код проекта распространяется по лицензии **Apache License 2.0**.

- Полный текст лицензии: [LICENSE](./LICENSE).
- Уведомления о сторонних компонентах: [NOTICE](./NOTICE) (marked / KaTeX / highlight.js / DOMPurify / jsPDF / html2canvas-pro / turndown / mermaid / @retorquere/bibtex-parser и Tauri и т. д., каждый под MIT / BSD-3-Clause / Apache-2.0 / MPL-2.0).
- Согласно Apache-2.0, при распространении необходимо сохранять LICENSE и NOTICE, а также указывать изменения в модифицированных файлах.

---

## 📬 Контакты

По проблемам или предложениям: **郑法伟 (Fawei Zheng) <fwzheng@bit.edu.cn>**
